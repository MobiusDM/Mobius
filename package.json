{
  "name": "mobius-commitlint",
  "private": true,
  "description": "Commitlint configuration for Mobius repository",
  "scripts": {
    "commitlint": "commitlint --from=HEAD~1 --to=HEAD --verbose",
    "commitlint-ci": "commitlint --from=$CIRCLE_SHA1~1 --to=$CIRCLE_SHA1 --verbose"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.0.0",
    "@commitlint/config-conventional": "^18.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}