# binaries
*.exe
*.dll
*.so
*.dylib
*.bin
*.out
mobius
/mobiuscli
client
cocoon

# output directories
/build
vendor
tmp/

# operating system artifacts
.DS_Store

# test mysql server data
mysqldata/

# test helm charts
helm-temp
charts/mobius/charts

#editors
.idea
.swp

# Mobius local development DB backups
backup.sql.gz

# infra
.terraform
.terraform.tfstate*
terraform.tfstate*

# generated orbit installers and artifacts
mobius-osquery*
desktop.app.tar.gz
osqueryd.app.tar.gz
desktop.tar.gz
nudge.app.tar.gz
swiftDialog.app.tar.gz

# residual files when running the cpe command
cmd/cpe/etagenv
cmd/cpe/cpe*.sqlite
cmd/cpe/cpe*.sqlite.gz


# Residual files when running the msrc generate command
msrc_in/
msrc_out/

# Residual files when running cmd/macoffice/generate.go
macoffice_rel_notes/

# Keys and certificates that may be generated in the root of the repo
# (e.g. with ./build/mobiuscli generate ...).
/*.key
/*.crt
/*.pem

# IDE
.vscode
.cursor

# Residual files from osqueryd loadtests.
osquery_worker_*.jpg

# Residual files when building mobiusdaemon_tables extension.
mobiusdaemon_tables_*

# Residual files when building mobius_tables extension.
mobius_tables_*.ext

# Archives and large artifacts not tracked
*.tar
*.tar.gz
*.zip
legacy-backend-archive.tar.gz

mobius-server/mobius-api
mobius-server/static/


# Node.js dependencies for commitlint
node_modules/
package-lock.json

